package home_work;

import java.time.LocalDate;
import java.time.temporal.ChronoUnit;
import java.util.HashMap;
import java.util.stream.IntStream;
import java.util.stream.Stream;

import static java.time.DayOfWeek.FRIDAY;

/**
 * @author Evgeny Borisov
 */
public class BlackFridayUtil {
    public static void printBlackFridayYearsSorted(int start, int end) {
        HashMap<Object, Object> objectObjectHashMap = new HashMap<>();
//        objectObjectHashMap.entrySet()

//        LocalDate startDate = LocalDate.of(1, 13, start);
//        ChronoUnit.MONTHS.between()
//        Stream.iterate(startDate,date->date.plusMonths(1))




//        IntStream.range(1,12)
//        Stream<LocalDate> dateStream;
//        dateStream.filter(localDate -> localDate.equals(FRIDAY))
//                .filter()
    }
}
